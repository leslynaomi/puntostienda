import 'package:flutter/material.dart';
import 'package:puntotienda/bottom_bar.dart';

class MainScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return PageView(

    //children: [BottomBarScreen(),UploadPructForm()],

    );
  }
}
